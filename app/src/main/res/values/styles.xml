<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="radioButtonStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:layout_marginBottom">6dp</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:drawableRight">@null</item>
    </style>

    <style name="ColorPickerDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog" />

    <style name="SnackBarStyle" parent="Widget.Material3.Snackbar">
        <item name="android:backgroundTint">?attr/colorSurfaceVariant</item>
    </style>

    <style name="SnackBarStyle.TextView" parent="Widget.Material3.Snackbar.TextView">
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
    </style>

    <style name="SnackBarStyle.Button" parent="Widget.Material3.Button.TextButton.Snackbar">
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>
</resources>